# Local Docker Compose Postgres only — non-production placeholder credentials.
# Copy to .env (gitignored) for local development.
DATABASE_URL=postgres://siliconskunk:siliconskunk_dev_only@localhost:5432/siliconskunk

# Demo-only (TASK-18): forces the mocked notification provider to always
# fail, for demonstrating the retry/backoff and terminal-failure paths.
# Boot-time only — no HTTP toggle. Uncomment and restart the API to enable.
# NOTIFICATION_DEMO_FORCE_FAIL=true
