# Local Docker Compose Postgres only — non-production placeholder credentials.
# Copy to .env (gitignored) for local development.
DATABASE_URL=postgres://siliconskunk:siliconskunk_dev_only@localhost:5432/siliconskunk
